---
alwaysApply: true
description: Configuración general del proyecto y tecnologías principales
---

# Gestor de Proyectos - Configuración del Proyecto

## Gestor de Paquetes

- **SIEMPRE usa pnpm** como gestor de paquetes
- Nunca usar npm o yarn
- Para instalar dependencias: `pnpm install`
- Para agregar paquetes: `pnpm add <package>`
- Para ejecutar scripts: `pnpm run <script>`

## Stack Tecnológico Principal

- **Framework**: Next.js 15.5.7 (App Router)
- **UI Framework**: React 19.1
- **Base de datos**: PostgreSQL con Prisma ORM 6
- **Autenticación**: NextAuth.js v4 (Credentials + Prisma adapter)
- **Estilos**: TailwindCSS 4
- **Componentes UI**: shadcn/ui (new-york)
- **Drag & Drop**: @dnd-kit
- **Testing**: Vitest + Testing Library (Strict TDD vía OpenSpec)
- **Deploy**: Supabase (PostgreSQL)

## Estructura del Proyecto

```
src/
├── app/                    # Next.js App Router
│   ├── api/               # API routes (auth, downloads, maintenance…)
│   ├── auth/              # Páginas de autenticación
│   ├── inicio/            # Portal personal
│   ├── dashboard/         # Dashboard principal
│   ├── proyectos/         # Gestión de proyectos
│   ├── fondos/            # Operaciones por fondo
│   ├── novedades/         # Novedades / convocatorias
│   └── configuracion/     # Ajustes (Admin)
├── components/            # Componentes React
│   ├── ui/               # Componentes base de shadcn/ui
│   ├── portal/           # Portal Inicio
│   └── proyectos/        # Componentes específicos de proyectos
├── lib/                  # Utilidades y configuración
│   ├── actions/          # Server Actions de Next.js
│   ├── authz/            # Guards de autorización
│   ├── permissions/      # Matriz de permisos
│   ├── auth-actions.ts   # Acciones de autenticación
│   └── prisma.ts         # Cliente de Prisma
├── hooks/                # Custom React hooks
└── types/                # Definiciones de tipos TypeScript

openspec/                 # Constitución SDD + changes
prisma/
├── schema.prisma         # Schema de base de datos
└── migrations/           # Migraciones de Prisma
```

## Convenciones de Código

### Componentes React

- Usar **'use client'** cuando el componente necesita interactividad
- Componentes de servidor por defecto (sin directiva)
- Nombres de archivos en PascalCase para componentes
- Exportación por defecto para componentes de página

### Server Actions

- Ubicar en `src/lib/actions/`
- Siempre incluir **'use server'** al inicio
- Usar guards de `@/lib/authz` (`requireSession` / `requireAdmin` / `requirePermission` / `requireProjectAccess`)
- Usar `revalidatePath()` después de mutaciones
- Retornar objetos con formato `{ success: boolean, data?: T, error?: string }`

### Base de Datos

- Usar Prisma para todas las operaciones de BD
- Ejecutar migraciones con: `pnpm exec prisma migrate dev --name <nombre>`
- Generar cliente después de cambios: `pnpm exec prisma generate`
- **PROHIBIDO**: eliminar datos de BD, ejecutar/crear seeds, o insertar datos ficticios (ver regla `no-db-destructive-or-seed`)
- Los archivos `prisma/seed*.ts` están en cuarentena: **nunca ejecutarlos**

### Estilos

- Usar clases de Tailwind directamente
- Componentes UI de shadcn/ui en `src/components/ui/`
- Colores principales: emerald (éxito), blue (primario), gray (neutral)

## Scripts Importantes

- `pnpm dev` - Iniciar servidor de desarrollo
- `pnpm build` - Construir para producción
- `pnpm start` - Iniciar servidor de producción
- `pnpm test` - Tests Vitest (Strict TDD)
- `pnpm test:coverage` - Cobertura
- `pnpm exec prisma studio` - Interfaz visual de base de datos
- `pnpm exec prisma migrate dev` - Crear y aplicar migraciones

## Variables de Entorno Requeridas

- `DATABASE_URL` - URL de conexión a PostgreSQL
- `NEXTAUTH_SECRET` - Secret para NextAuth
- `NEXTAUTH_URL` - URL de la aplicación
- `CONFIG_UNLOCK_PASSWORD` - Desbloqueo del panel Admin (usuarios/proyectos); sin default
- Novedades/Soporte: contraseña fija `bitacora` en código (no usa env)
