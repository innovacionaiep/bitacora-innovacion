# Tasks

1. Pure helper + Vitest (`linea-modulos.ts`)
2. Prisma flags on Linea + additive migration copying fondo flags
3. Server actions `getLineasModulosMatrix`, `setLineaTabEnabled`, `getLineasTabsCatalog`
4. Config page `/configuracion/lineas` + nav item
5. Wire ProyectosContent + Convenio/Escalamiento gates
6. Fondos/dashboard convenio filtering and KPIs
7. Remove fondo On/Off tables and DT lineas sheet
