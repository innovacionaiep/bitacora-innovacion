# Tasks: proyecto-tab-igip-trl

1. OpenSpec change + Vitest for `igip-trl` domain (labels, validation, radar, TRL appearance)
2. Additive Prisma: `tabIgipTrlEnabled` + `ProyectoIgip`
3. Register `IgipTrl` in `linea-modulos` (catalog, flags, tests, config matrix)
4. Server actions, query key, prefetch
5. Nav + `IgipTrlTab` / `IgipTrlCard`
6. `pnpm test` on touched files; manual tab / persist / flag Off
