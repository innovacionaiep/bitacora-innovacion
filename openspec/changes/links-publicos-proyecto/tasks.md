# Tasks

1. Pure helpers + Vitest (`public-link.ts`)
2. Prisma model + additive migration + unique partial index
3. Config actions + tab UI (TDD)
4. `requireProjectReadAccess` on GET loaders; download APIs
5. `/p/[token]` route, middleware, RouteAwareShell
6. `ProyectoPublicoFicha` read-only tabs
