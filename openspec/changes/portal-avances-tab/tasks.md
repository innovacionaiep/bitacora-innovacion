# Tasks: portal-avances-tab

1. Lib pura: lista de fondos, filtros, escuelas, sort, métricas de avance (tests Vitest)
2. Server action `getPortalAvancesProyectos` (gate portal, solo ID y RIE)
3. `VitrinaAvancesView` + sidebar `hiddenFacets`
4. Cablear SSR en `(portal)/page.tsx` y `VitrinaLanding`
5. `pnpm test` en archivos tocados
