# Tasks: portal-avances-vcm-excel

1. OpenSpec (este change)
2. Catálogo: Vinculación `excel`; Causalab filtra por `PORTAL_CAUSALAB_FONDO` (Vitest)
3. Parser/snapshot: key `portal_avances_vcm`, hoja default `Fondo VcM`, ids `vcm:` (Vitest)
4. Actions admin VcM + merge en `getPortalAvancesProyectos` (Vitest, mocks)
5. Tab Configuración del portal + columnas Excel en Avances (Testing Library)
6. `pnpm test` en archivos tocados
